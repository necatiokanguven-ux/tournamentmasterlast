# ID Scan API (VPS only — do NOT put Gemini key in Tournament Master salon installs)
# Deploy vps/id-scan on api.pokerclup.com with GEMINI_API_KEY set there.
#
# Optional override for local dev (point salon app at local id-scan service):
# ID_SCAN_API_URL=http://127.0.0.1:3010/api/id-scan
